<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FootballClub" parent="Theme.Material3.DynamicColors.Light">
        <!-- Primary brand color. -->
        <item name="background">@color/background</item>
        <item name="colorPrimary">@color/fab_color</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/background</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- стиль для текста -->
    <style name="TextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="FabTheme" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50dp</item>
        <item name="android:radius">40dp</item>
    </style>

    <style name="Theme.LawsRB.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="elevation">0dp</item>
    </style>

    <style name="Theme.LawsRB.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
    </style>

    <style name="Theme.LawsRB.CardView.Title" parent="Widget.MaterialComponents.TextView">
        <item name="android:text">@string/e_mail</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.ftclub.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:layout_margin">3dp</item>
        <item name="borderWidth">0dp</item>
        <item name="strokeWidth">0dp</item>
        <item name="android:textSize">20sp</item>

    </style>

    <style name="Theme.ftclub.CardView.Card.Content" parent="Theme.ftclub.CardView">
        <item name="backgroundColor">#FFFBFE</item>
        <item name="cardElevation">0dp</item>
        <item name="cardCornerRadius">0dp</item>
    </style>

    <style name="Theme.ftclub.CardView.AboutPlayer" parent="Theme.ftclub.CardView">
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="cardElevation">0dp</item>
        <item name="cardCornerRadius">0dp</item>
    </style>


    <style name="MaterialAlertDialog.Rounded" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="shapeAppearanceOverlay">
            @style/ShapeAppearanceOverlay.MaterialAlertDialog.Rounded
        </item>
    </style>

    <style name="ShapeAppearanceOverlay.MaterialAlertDialog.Rounded" parent="">
        <item name="cornerSize">10dp</item>
    </style>

    <style name="Theme.FootballClub.SearchView" parent="Widget.AppCompat.Light.SearchView">
        <item name="submitBackground">@color/transparent</item>
        <item name="queryBackground">@color/transparent</item>
    </style>

    <style name="ThemeOverlay.AppTheme.TextInputEditText.Outlined" parent="">
        <item name="colorControlActivated">@color/background_about_app</item>
    </style>

    <style name="ProgressBarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/black</item>
    </style>

    <style name="Theme.FootballClub.Application" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/fab_color</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/light_white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSecondaryContainer">@color/fill_color</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar" >true</item>
        <item name="android:statusBarColor" >@color/background</item>

        <item name="searchViewStyle">@style/Theme.FootballClub.SearchView</item>
    </style>
</resources>